{% extends "NOFrontBundle::base.html.twig" %}

{% block title %}Index - {{ parent() }}{% endblock %}

{% block body %}
<h1>Noviris/Test - Administration</h1>
<div class="row">
	<div class="col-md-12"><p>Tous les internautes</p>
<!-- mettre le tableau dans un fichier séparé mais pas le temps d'apprendre tout cela !	-->
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Nom</th>
        <th>Prenom</th>
        <th>Email</th>
        <th>Date de naissance</th>
        <th>Marque</th>
        <th>Modele</th>
      </tr>
    </thead>
    <tbody>
	    {% for internaute in all_internautes %}
      <tr>
        <td>{{internaute.getNom()}}</td>
        <td>{{internaute.getPrenom()}}</td>
        <td>{{internaute.getEmail()}}</td>
        <td>{{internaute.getDatedenaissancelabel()}}</td>
        <td>{{internaute.getMarquelabel()}}</td>
        <td>{{internaute.getModelelabel(internaute.getMarque())}}</td>
      </tr>
		{% endfor %}
    </tbody>
  </table>
<!-- mettre le tableau dans un fichier séparé mais pas le temps d'apprendre tout cela !	-->
	
	</div>
</div>
{% endblock %}